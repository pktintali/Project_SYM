import 'package:flutter/material.dart';

class DraftMisCard extends StatelessWidget {
  const DraftMisCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
